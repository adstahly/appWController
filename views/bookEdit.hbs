<h1>Edit: {{book.title}}</h1>


<form action="/books/{{book.id}}?_method=PUT" method="POST">
    <p>
        <label>Title<br>
            <input name="title" value="{{book.title}}">
        </label>
    </p>
    {{#if errors.titleError}}<p class="errors">{{errors.titleError}}</p>{{/if}}
    <p>
        <label>Author<br>
            <input name="author" value="{{book.author}}">
        </label>
    </p>
    {{#if errors.authorError}}<p class="errors">{{errors.authorError}}</p>{{/if}}
    <p>
    <label>Pages<br>
        <input name="pages" type="number" value="{{book.pages}}">
    </label>
    </p>
    {{#if errors.pagesError}}<p class="errors">{{errors.pagesError}}</p>{{/if}}
    <p>
        <label>ISBN (optional)<br>
            <input name="isbn" value="{{book.isbn}}">
        </label>
    </p>
    <p>
        <button type="submit">Save</button>
        <a href="/books/{{book.id}}">Cancel</a>
    </p>
</form>

<form action="/books/{{book.id}}?_method=DELETE" method="POST" style="margin-top:1rem"
                              onsubmit="return confirm('Delete this book?');">
    <button type="submit">Delete</button>
</form>

<p><a href="/books">Back to list</a></p>
